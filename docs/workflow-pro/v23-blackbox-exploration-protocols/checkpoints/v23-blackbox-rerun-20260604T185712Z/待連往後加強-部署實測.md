# 待連往後加強：部署實測

Run: v23-blackbox-rerun-20260604T185712Z

這輪依照要求以 localhost 為主通道完成黑盒探索、修復前置與實證。若要把「push 後網路實際狀態」做得更完整，下一輪建議新增 production/preview parity：

| ID | 建議 | 原因 |
|---|---|---|
| DEP-001 | 部署到 Vercel preview，重新跑 Providers panel / Global API Vault / Workflow runtime / generated history / download | 驗證本機環境與 preview runtime 的 env、auth、storage、provider 行為一致。 |
| DEP-002 | 用新帳號在 preview 做完整 UI sign-up/login + LLM stream | 目前三帳號已完成 API/provider 層，preview UI 層仍未驗證。 |
| DEP-003 | 在 preview 跑 image-gen + artifact download，再與 localhost artifact 行為比較 | 確認 storage URL、download headers、workspace permissions 不只在 localhost 成立。 |
| DEP-004 | 檢查 Vercel build/runtime logs，記錄 request id、trace id、status code，不記錄 raw secret | 補足部署層可觀測性。 |
| DEP-005 | 在 preview 分別跑 single START 與 START ALL | localhost 已證明 single START 可完成 LLM+img2，但 START ALL 多起點在 Lite Runner 失敗；preview 必須檢查是否同樣失敗或由不同 runner 處理。 |

本輪不把部署狀態宣稱為通過。
